# Roombook verification contract.
build: dotnet build server/Roombook.slnx --nologo -warnaserror
test: dotnet test server/Roombook.slnx --nologo --no-build
lint: cd client && npm ci --no-audit --no-fund && npm run lint && npm run typecheck && npm test -- --run && npm run build
