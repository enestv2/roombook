# Leave empty for same-origin production deployments.
VITE_API_BASE_URL=
# The Vite development proxy forwards /api to this ASP.NET Core URL.
VITE_API_PROXY_TARGET=http://localhost:5113
# Development-only convenience authentication; never set this in production.
VITE_DEVELOPMENT_MEMBER_ID=
